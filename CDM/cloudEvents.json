{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "CloudEvents Specification JSON Schema. Use this schema to process events you receive from the NEN integration channels your're subscribed to. The schema defines the structure of the events, including required and optional attributes, as well as the data payload. Used for reference.",
  "type": "object",
  "properties": {
    "id": {
      "description": "Identifies the event.",
      "$ref": "#/definitions/iddef",
      "examples": [
        "A234-1234-1234"
      ]
    },
    "source": {
      "description": "Identifies the context in which an event happened.",
      "$ref": "#/definitions/sourcedef",
      "examples" : [
        "https://github.com/cloudevents",
        "mailto:cncf-wg-serverless@lists.cncf.io",
        "urn:uuid:6e8bc430-9c3a-11d9-9669-0800200c9a66",
        "cloudevents/spec/pull/123",
        "/sensors/tn-1234567/alerts",
        "1-555-123-4567"
      ]
    },
    "specversion": {
      "description": "The version of the CloudEvents specification which the event uses.",
      "$ref": "#/definitions/specversiondef",
      "examples": [
        "1.0"
      ]
    },
    "type": {
      "description": "Describes the type of event related to the originating occurrence.",
      "$ref": "#/definitions/typedef",
      "examples" : [
        "com.github.pull_request.opened",
        "com.example.object.deleted.v2"
      ]
    },
    "datacontenttype": {
      "description": "Content type of the data value. Must adhere to RFC 2046 format.",
      "$ref": "#/definitions/datacontenttypedef",
      "examples": [
        "text/xml",
        "application/json",
        "image/png",
        "multipart/form-data"
      ]
    },
    "dataschema": {
      "description": "Identifies the schema that data adheres to.",
      "$ref": "#/definitions/dataschemadef"
    },
    "subject": {
      "description": "Describes the subject of the event in the context of the event producer (identified by source).",
      "$ref": "#/definitions/subjectdef",
      "examples": [
        "mynewfile.jpg"
      ]
    },
    "time": {
      "description": "Timestamp of when the occurrence happened. Must adhere to RFC 3339.",
      "$ref": "#/definitions/timedef",
      "examples": [
        "2018-04-05T17:31:00Z"
      ]
    },
    "data": {
      "description": "The event payload.",
      "$ref": "#/definitions/datadef",
      "examples": [
        "<much wow=\"xml\"/>"
      ]
    },
    "data_base64": {
      "description": "Base64 encoded event payload. Must adhere to RFC4648.",
      "$ref": "#/definitions/data_base64def",
      "examples": [
        "Zm9vYg=="
      ]
    }
  },
  "required": ["id", "source", "specversion", "type"] ,
  "definitions": {
    "iddef": {
      "type": "string",
      "minLength": 1
    },
    "sourcedef": {
      "type": "string",
      "format": "uri-reference",
      "minLength": 1
    },
    "specversiondef": {
      "type": "string",
      "minLength": 1
    },
    "typedef": {
      "type": "string",
      "description": "Format: -Should be reverse-DNS style to avoid collisions (e.g., com.example.object.created). -Can include versioning (e.g., com.example.object.created.v2). -Must be a non-empty string. -Should be stable over time for the same event type",
      "minLength": 1
    },
    "datacontenttypedef": {
      "type": ["string", "null"],
      "minLength": 1
    },
    "dataschemadef": {
      "type": ["string", "null"],
      "format": "uri",
      "minLength": 1
    },
    "subjectdef": {
      "type": ["string", "null"],
      "minLength": 1
    },
    "timedef": {
      "type": ["string", "null"],
      "format": "date-time",
      "minLength": 1
    },
    "datadef": {
        "$comment": "check what kind of data goes in here, and if we can be more specific about the type",
        "type": "string"
    },
    "data_base64def": {
      "type": ["string", "null"],
      "contentEncoding": "base64"
    },
    "nenTopic": {
      "description": "Topics for NEN events, used to categorize the type of event and its relevance to subscribers.",
      "anyOf": [
        { "$ref": "#/definitions/committeeTopic" },
        { "$ref": "#/definitions/publicationTopic" }
      ]
    },
    "publicationTopic": {
      "type": "string",
      "enum": [
        "family",
        "familyAdmin",
        "project",
        "publication",
        "releaseItem"
      ]
    },
    "committeeTopic": {
      "type": "string",
      "enum": [
        "committee",
        "member"
      ]
    },
    "subscriptionRequest": {
      "type": "object",
      "description": "Request body for creating an AMQP subscription to CloudEvents",
      "properties": {
        "source": {
          "description": "URI reference identifying the source of the events to subscribe to. Must adhere to RFC 3986.",
          "$ref": "../CDM/NenDefinitions.json#/definitions/url"
        },
        "type": {
          "description": "The type of the events to subscribe to. Must adhere to CloudEvents type format. Format: -Should be reverse-DNS style to avoid collisions (e.g., com.example.object.created). -Can include versioning (e.g., com.example.object.created.v2). -Must be a non-empty string. -Should be stable over time for the same event type.",
          "type": "string",
          "minLength": 1
        },
        "address": {
          "description": "Address of the AMQP broker to subscribe to, including protocol, host, port and path. Must adhere to RFC 3986.",
          "$ref": "../CDM/NenDefinitions.json#/definitions/url" 
        },
        "linkName": {
          "type": "string" 
        },
        "senderRole": {
          "description": "The role being played by the peer, i.e., whether the peer is the sender or the receiver of messages on the link",
          "type": "string",
          "enum": ["sender", "receiver"]
        },
        "senderSettlementMode": {
          "description": "The settlement mode to be used by the sender. This indicates when messages are considered 'settled' (i.e., successfully processed) and can be used for flow control and reliability guarantees.",
          "type": "string",
          "enum": ["settled", "unsettled"]
        },
        "linkProperties": {
          "type": "object",
          "additionalProperties": {
              "type": "string"
          }
        }
      },
      "required": ["source", "type"],
      "additionalProperties": false
    },
    "subscriptionList": {
      "type": "array",
      "description": "a list of subscriptions",
      "items": {
        "$ref": "#/definitions/subscription"
      }
    },
    "subscription": {
      "type": "object",
      "title": "Subscription",
      "properties": {
        "id": {
          "$ref": "#/definitions/handle"
        },
        "clientId": {
          "type": "string",
          "description": "The unique identifier of the client that created the subscription.",
          "example": "my-client-id"
        }
      },
      "additionalProperties": false
    },
    "handle": {
      "type": "string",
      "description": "The unique identifier of the subscription in the scope of the subscription manager. An AMQP alias established by the attach frame and subsequently used by endpoints as a shorthand to refer to the link in all outgoing frames.",
      "example": "1119920371"
    }
  }
}